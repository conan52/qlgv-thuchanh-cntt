@if (!Request.IsAuthenticated)
{
@*    @Html.ActionLink("Tạo tài khoản", "Register", "Account")*@
@*    @: | *@
    @Html.ActionLink("Đăng nhập", "Login", "Account")
}
else
{
    @: Xin chào <b>@User.Identity.Name</b>!

    @: |
    @Html.ActionLink("Thoát", "LogOff", "Account")
}